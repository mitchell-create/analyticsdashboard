# Metabase on Railway — uses Postgres add-on for Metabase app DB
# Set MB_DB_* from Railway Postgres add-on variables

FROM metabase/metabase:v47.6.6

# Metabase reads MB_DB_TYPE, MB_DB_DBNAME, MB_DB_PORT, MB_DB_USER, MB_DB_PASS, MB_DB_HOST from env
# No custom config needed — all via env vars
